# WhatsApp session data — never commit these
auth_session/
auth_sessions/

# npm dependencies — installed fresh on each server
node_modules/

# package-lock is committed so fresh installs use exact dependency versions
# Logs
*.log
